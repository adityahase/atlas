Reuse as many components as possible.

Any UI code is code we have to keep up to style and maintain.

Don't write any UI code. Review all UI code and find how can we replace it with standard components. All we should do is pass parameters to the components. That's it.

Review all UI code thoroughly

Use frappe-dev and frappe-ui skills. Refer to llm/references to see how other apps avoid UI code.
Show less